# AGENTS.md

## Cursor Cloud specific instructions

This is currently an empty repository (`cursor-tower`) with no application code, dependencies, or services. The repo contains only a `README.md`.

When code is added to this repository, update this section with:
- How to install dependencies
- How to run, build, lint, and test the application
- Any non-obvious environment setup caveats
